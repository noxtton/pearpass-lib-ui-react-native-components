name: Test check

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Configure Git credentials
        run: |
          git config --global --add url."https://${{ secrets.PAT_TOKEN }}:x-oauth-basic@github.com/".insteadOf "git+ssh://git@github.com/"
          git config --global --add url."https://${{ secrets.PAT_TOKEN }}:x-oauth-basic@github.com/".insteadOf "git@github.com:"
          git config --global --add url."https://${{ secrets.PAT_TOKEN }}:x-oauth-basic@github.com/".insteadOf "https://github.com/"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: "https://npm.pkg.github.com"
          scope: "@tetherto"

      - name: Checkout peer dependencies from GitHub
        run: |
          PEER_DEPS=$(node -pe "Object.keys(require('./package.json').peerDependencies || {}).filter(dep => dep.startsWith('pearpass-')).join(' ')")
          for dep in $PEER_DEPS; do
            echo "Checking out $dep..."
            git clone https://${{ secrets.PAT_TOKEN }}@github.com/noxtton/$dep.git $dep
          done

      - name: Install dependencies and peer dependencies
        run: |
          npm ci --legacy-peer-deps
          
          # First, install any peer deps from registry
          PEER_DEPS=$(node -pe "Object.keys(require('./package.json').peerDependencies || {}).join(' ')")
          echo "PEER_DEPS found: $PEER_DEPS"
          for dep in $PEER_DEPS; do
            if [ ! -d "./$dep" ]; then
              echo "Installing $dep from registry..."
              npm install $dep --legacy-peer-deps --no-save
            fi
          done
          
          # Then, copy local peer deps
          for dep in $PEER_DEPS; do
            if [ -d "./$dep" ]; then
              echo "Copying local $dep to node_modules..."
              cp -r ./$dep ./node_modules/$dep
            fi
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.PAT_TOKEN }}

      - name: Run tests
        run: npm test